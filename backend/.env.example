# Server configuration
PORT=3000
NODE_ENV=development

# Database connection (Prisma expects a PostgreSQL URL)
DATABASE_URL="postgresql://postgres:root@localhost:5433/footalent?schema=public"

# Authentication
JWT_SECRET=1XUwVKfnhJa43PTxZT7EB5lXYtxpktMFcvN7klvX2bo

# Swagger documentation
SWAGGER_HOST=http://localhost:3000
SWAGGER_SCHEMES=http

# CORS
# Comma-separated list of allowed origins; use * to allow any origin (not recommended for production)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
